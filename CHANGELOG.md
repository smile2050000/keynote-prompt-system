# 更新记录

## v0.9.3 - 2026-08-21

- Storyboard 从基础摘要扩展为内容任务、视觉关系、产品约束和风险分组，关键字段必须在生成前真实落盘。
- Prompt 编译加入具体构图简报、证据类型、主视觉载体、空间语法和跨页差异；每页必须完整注入 Global Visual Contract，不能用简称代替。
- 生产模式固定为 Image2 整页生图，不再自动降级为底图排字；无产品页使用 `generate_image`，出现目标产品时必须使用 Reference 并调用 `edit_image`。
- 加强产品页的镜头、朝向、数量、尺度、场景因果和产品身份规则，减少同角度重复、朝向错误和凭空生成产品。
- QA 增加准确文字与数字、产品 Reference、Global Contract 和 Storyboard 关系核对；只定点重生问题页。
- PPTX 改为用户明确需要时由下游 `slides` / `ppt` Skill 封装，不再与主 Skill 的规划职责混在一起。
- 校准中性现代家居和浅紫高端女性消费风格，强化商业广告摄影、柔和专业布光、真实材质与清洁中间调，抑制灰雾、过曝、暖黄和低龄化表现。
- 增加“每轮独立开始”规则：新目录、新来源、新 Storyboard、新 Prompt；历史内容默认不可见，只有用户明确指定的 Reference 才能继承。
- 新增完整团队教程和 README 排查说明，并通过摄像头项目与闺蜜机项目的 2K 流程测试持续校准规则。

## v0.9.1 - 2026-08-19

- 将风格选择收敛为 `style-presets.md` 唯一入口，避免 Style Preset、Palette 和项目记录并列造成混乱。
- 正式整理五个方向：中性现代家居、现代北欧都市家庭、浅紫高端女性消费、年轻都市消费电子、深色科技发布会。
- 为每个方向新增 `validated / project-validated / experimental` 状态；深色科技发布会在完成整套验证前仍要求先做 1–2 页试样。
- 将旧 `palette-systems.md` 降级为术语兼容表；旧深色家居与主会场命名不再作为独立主风格。

## v0.9.0 - 2026-08-19

- 新增 `storyboard.md`，在逐页 Prompt 前固定核心信息、观众记忆、Page Type、视觉强弱和特殊风险，但不规定死版式。
- 将 Global Visual Contract 独立为 `global-visual-contract.md`，作为跨页视觉规则唯一来源；旧 `design-system.md` 保持兼容输入。
- 明确逐页 Prompt 声明 Storyboard 与共享 Contract 来源，并在 Image2 调用时原样注入完整 Contract。
- 新增首轮 Contact Sheet、`qa/qa.md` 和最终 Contact Sheet；QA 只记录明显问题，按页定点重生，不默认全套重跑。
- 预留 approved image 到 editable reconstruction 的轻量交接接口，不引入 Layout Library、自动评分或多 Agent 路由。

## v0.8.0 - 2026-08-18

- 将 A 风格从 `warm-home-editorial` 校准为 `neutral-home-keynote`，旧 ID 保留为兼容别名。
- 建立 70% 中性白灰、20% 真实家居材质、10% 深色视觉锚点的色彩基线；产品颜色始终服从真实 Reference。
- 木色降为局部材质点缀，禁止黄色滤镜、黄金时刻、昏黄灯光和棕黄阴影。
- 明确温馨来自人物行为、产品作用和家庭关系；封面、产品页与数据页可使用抽象发布会空间。

- 收敛 v0.8 规则结构：`SKILL.md` 只保留流程，`style-presets.md` 只负责 Deck 级风格，`type-and-visual-grammar.md` 只负责信息页语法，`prompt-schema.md` 只负责 Prompt 组装。
- 删除核心规则中的项目专属产品路径、Butler/Captain/Guard 示例和测试数据，统一由项目 `content-brief.md` 与 `prompt-pack.md` 承担。
- 修复深浅背景文字颜色冲突、标题结构命名不一致和“缺失内容发送待确认”等执行风险。
- 将重复负面约束压缩为少量最高风险类别，保留已验证的通透背景、自然文字安全区、语义视觉载体和背景/排版解耦原则。

- v0.8 新增 Style Lock：每套 Deck 只激活一个 `style_id`，默认禁止跨页混搭。
- v0.8 将 B 收敛为 `neutral-home-keynote-dark`：A 中性现代家居发布会的深色主题变体，严格继承浅色版版式和图形逻辑；旧 ID 保留为兼容别名。
- v0.8 明确禁止 B 通过黑色异形块、黑色中轴、黑色金属和硬分割表达风格。
- C 风格改为 `designed-keynote-stage`，先完成小米发布会参考文件第二轮提取，暂不直接测试。

- 重构为 V1 短链路：Deck Plan、Global Visual Contract、Page Type、Per-page Prompt、Image2、contact sheet review。
- Page Type 只定义信息意图，不再承担固定布局；移除 V1 默认流程中的多层视觉逻辑拼装。
- 新增逐页真实执行日志要求，保存 Prompt、Reference、调用参数、模型和输出结果。
- 强制所有出现目标产品的页面使用统一 contact sheet，并通过 `edit_image + strength: low` 生成，禁止模型凭空生成相似产品。
- 将内部设计规划与最终 Image2 Prompt 分离，最终 Prompt 只保留模型可执行的空间、对象、色彩、成像和文字指令。
- 产品资产页强制路由到 `edit_image`，默认低强度编辑；禁止使用通用文生图模拟图生图。
- 删除雾白、光学路径、舞台状态等容易造成灰雾、乱线或抽象误读的默认表达。
- 增加影调曝光、广告画质、真实材质和产品资产优先规则。
- 主标题统一深石墨黑，副标题与说明统一中性灰；取消标题关键词换色。
- 限制纯白过曝、涂抹感、镜面展台、透明圆环和过强反射等 AI 科技海报倾向。
- 新增发布会视觉逻辑层：页面动作、唯一主视觉对象、视觉隐喻、最小文字集合和减法契约。
- 增加字体契约：中文苹方/思源黑体，英文数字 Arial/Helvetica Neue 风格，主标题黑色、副标题灰色。
- 细化产品资产图生图契约，明确允许变化项与不可变化项，避免输入 PNG 被模型重绘成相似产品。
- 新增三种默认标题结构：左右居中上方、左右排布上下居中、金句居中；特殊结构必须记录原因。
- 新增图表/流程/逻辑/数据页的 `Visual Mode`，默认采用平面编辑化信息设计，不把玻璃材质和发光 3D 作为默认答案。
- 增加产品 Reference 的“仅识别、不展示”规则，禁止 contact sheet、六面图和多角度阵列直接出现在最终页面。
- 增加场景摄影、产品融合、图表信息设计三个可选质量后缀，按页型选择，不叠加全部后缀。
- 增加整套预览输出规则：完整 `montage-all.png` 和图片铺满的 `deck-preview.pptx`。
- 增加 Typography Contract：字体、相对字号层级、行距、边距、标题区域和黑灰文字颜色跨页稳定，禁止彩色标题。
- 增加 Editorial Visual Grammar：图表、流程、逻辑、数据页必须有内容驱动的视觉隐喻和统一图形语法，避免图库图标行与模板卡片墙。
- 优化 Editorial Visual Grammar：从“扁平和少元素”调整为“主关系可见、尺度/密度/方向有变化、允许克制空间层次”，避免过度素、死板和图库素材感。
- 增加场景与文字关系规则：使用真实空间留白、明确硬裁切或完整叠字，禁止白色渐隐和雾化软分割。
- 收紧“规则引擎 → 任务引擎”流程页：表达能力结构升级，不得生成管道、河流、轨道、物流路径、箭头海洋或底部标签栏。

## v0.5.1 - 2026-08-13

### 调整

- 回退为质量优先、弱约束的提示词策略，作为当前默认版本。
- 移除三种区块母型、20% 留白、人物页数、标题字数等硬性覆盖规则。
- 保留浅色基础方向、中国家庭人物、事实校验、标题气质和明显错误禁止项。
- 明确以“全局气质 + 层级 + 色彩边界 + 叙事节奏”控制整套统一性，不强制逐页模板。
- v0.5.0 仍保留在 Git 历史中，供后续对比测试。

## v0.5.0 - 2026-08-13

### 新增

- 建立 `keynote-prompt-system` 独立 Skill 仓库。
- 支持 txt、md、html、docx、pdf、pptx、截图及其他可读取文本格式。
- 增加单一主要需求文件和参考材料的来源规则。
- 增加需求冲突、页数不明、参数冲突、风格不明确时的确认闸门。
- 建立家用智能产品的浅色/深色基础系统和温暖家居、浅紫典雅、年轻生活、中性新品变体。
- 页面家族改为观点、场景、数据、能力、系统、产品、对比七类阅读任务。
- 增加整幅主图、左右双区、中心主体 + 周边短标签三种区块母型。
- 增加人物节奏、相邻页连续性、留白、标题字数和硬切控制规则。
- 明确规划阶段、Image2 生图阶段和 slides PPTX 交付阶段的职责边界。

### 已知限制

- Image2 对中文长标题、数字、额外小字和跨页产品外观复用仍不稳定。
- 尚未确定整页生图、底图后排字、母型 `edit_image` 三种模式的默认方案。
- 尚未建立自动化整套视觉评分和固定回归样例。
